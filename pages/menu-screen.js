class MenuScreen {
    draw() {
        fill(255)
        textAlign(CENTER, CENTER)

        textSize(50)
        text("MEU JOGO", width / 2, height / 3)

        textSize(20)
        text("Pressiona ENTER para começar", width / 2, height / 2)
    }
}